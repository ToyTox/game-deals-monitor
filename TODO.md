# TODO

Чек-лист известных проблем и задач. Подробности по коду — в [документации](docs/).

## Баги

- [ ] **`EpicParser.parseFreeGames()` — пустая заглушка.** Метод содержит только комментарий и всегда возвращает пустой массив, так что еженедельные раздачи Epic не собираются.
- [ ] **Free-to-play игры считаются бесплатными.** `isFree` выставляется по `currentPrice === 0`, поэтому в `/api/games/free` вместе с раздачами попадают F2P-тайтлы витрины.

## Улучшения

- [ ] **Аутентификация для `/api/admin/*`.** Сейчас раздел ничем не защищён: любой, кто до него дотянется, может запустить парсинг и прочитать статистику.
- [ ] **Асинхронный `POST /api/admin/parse`.** Ответ ждёт завершения всех сетевых запросов; для полного прогона это десятки секунд.
- [ ] **N+1 в статистике.** `getStats()` в цикле по платформам делает по два `count`-запроса на каждую — линейно растёт по числу площадок.
- [ ] **Переезд на PostgreSQL для Docker.** `docker-compose.yml` передаёт PostgreSQL-строку подключения, а проект настроен на SQLite — см. [docs/docker.md](docs/docker.md).
- [ ] **`parsePlatform()` ищет парсер по подстроке в имени класса.** Совпадение по `constructor.name.includes(platform)` — хрупко; нужно точное сопоставление по `getAvailablePlatforms()`. Поведение зафиксировано пином `БУДУЩИЙ БАГФИКС` в `tests/unit/parserService.test.ts` — правка требует инверсии пина в том же коммите.
- [ ] **`LOG_LEVEL` объявлена в `.env.example`, но в коде не используется.**
- [ ] **Выключение сервера.** Обработчик `SIGINT` отключается от базы и выходит, не закрывая http-сервер, а `SIGTERM` (его шлёт `docker stop`) не обработан вовсе.

## Ограничения by design

- **Цены разных площадок в разных валютах.** Steam заполняет `Game.currency` по коду региона (по умолчанию RUB), GOG ходит в каталог с `currencyCode=RUB`, а Epic отдаёт USD. Сравнивать абсолютные цены с Epic нельзя; корректны только скидки в процентах (`topDiscounts`, `averageDiscount`).
- **Пустой и закрытый вишлист неразличимы.** На `IWishlistService/GetWishlist` Steam отвечает `{"response":{}}` и когда список желаемого пуст, и когда он закрыт настройками приватности профиля. Различить случаи нечем, поэтому `GET /api/wishlist` отдаёт на оба один код `empty_or_private`, а текст ошибки приходится писать с оговоркой «пуст либо закрыт».
- **Валюта вишлиста берётся из региона, а не из ответа.** `IStoreBrowseService/GetItems` отдаёт цену отформатированной строкой (`«573 руб»`) без кода валюты, поэтому `currency` подставляется из `STEAM_COUNTRY_CODE`. Если переменная разойдётся с реальным регионом ответа, конвертация в рубли посчитает не то.
- **Второй источник Steam — HTML.** Список акций из `/search/results/` разбирается через cheerio по классам вёрстки. Если Valve поменяет разметку, источник тихо вернёт 0 игр — витрина при этом продолжит работать. Отключается через `STEAM_SEARCH_PAGES=0`.
